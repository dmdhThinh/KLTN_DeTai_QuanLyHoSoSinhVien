# Database Configuration
DB_HOST=your-database-host
DB_PORT=3306
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=your-database-name

# Server Configuration
PORT=10000
NODE_ENV=production

# JWT Secret (tạo một chuỗi ngẫu nhiên mạnh, ít nhất 32 ký tự)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# AWS S3 Configuration (cho upload file)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=ap-southeast-1
AWS_BUCKET_NAME=your-bucket-name

# CORS (URL frontend - sẽ cập nhật sau khi deploy frontend)
CORS_ORIGIN=https://your-frontend-domain.vercel.app

# Gemini API (nếu có sử dụng chatbot)
GEMINI_API_KEY=your-gemini-api-key

# Upload Configuration (nếu cần)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

